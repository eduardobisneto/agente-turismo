services:
  app:
    build: .
    image: greatgrandson-marketplace-web
    container_name: greatgrandson-marketplace-web-container
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
