services:
  app:
    build: .
    image: greatgrandson-agente-turismo
    container_name: greatgrandson-agente-turismo-container
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
